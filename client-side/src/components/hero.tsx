import { FC } from "react";

const Hero:FC = () => {
    return (
        <div className="hero">
            <div className="hero-wrapper">
                
            </div>
        </div>
    );
}

export default Hero;